{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Catalina",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rimraf dist && npm run build --prefix ../part2 && copyfiles -u 3 ../part2/dist/**/* dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"uibuild\" && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "nodemon": "^3.1.14",
    "rimraf": "^6.1.3"
  }
}
