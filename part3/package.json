{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Catalina",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && npm run build --prefix ../part2 && node -e \"require('fs').cpSync('../part2/dist','dist',{recursive:true})\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "nodemon": "^3.1.14",
    "rimraf": "^6.1.3"
  }
}
